class Solution {
    public int addDigits(int num) {
        int digit=0;
        while(true){
            int sum=0;
            while(num!=0){
                sum+=num%10;
                num/=10;
            }
            num=sum;
            if(sum<10)
            return sum;
        }
    }
}
